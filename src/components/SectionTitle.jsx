
const SectionTitle = ({ title }) => {
    return (
        <h1 className="text-[34px] text-darkBlue font-medium mx-auto leading-[54px] border-b-2 border-midBlue border-dashed px-2 w-fit">
            {title}
        </h1>
    );
};

export default SectionTitle;